# Intentional weight-regression acknowledgements.
#
# Format (one function/removal per line):
# <repo-relative-weight-path> <function>: <non-empty justification>
# <repo-relative-weight-path> *: <non-empty whole-file-removal justification>
#
# Example (keep commented unless the regression is intentional):
# runtime/bleavit-runtime/src/weights/pallet_constitution.rs set_param: storage proof grew after adding validation
# runtime/bleavit-runtime/src/weights/pallet_retired.rs *: pallet retired by approved migration
#
# (the B1b/B10 acknowledgements were consumed when those weights reached
# main — vs the post-#107 base there was no outstanding regression)
runtime/bleavit-runtime/src/weights/pallet_futarchy_treasury.rs execute_coretime_renewal: B12 (#111, merged into this branch) replaced the pre-activation placeholder with the real renewal path's conservative hand envelope — authenticated quote consumption + line debit + XCM RenewalDispatch send — pending the next omni-bencher regeneration sweep; inherited via the main merge, no weight change originates on this branch
